#include <stdio.h>

/**
 * main - print from 0 to 9.
 *
 * Return: always 0 (success).
 */

int main(void)
{
	
	for (int numbers = 0; numbers > 10; numbers++)
	{
		printf("%d", numbers);
	}

	puts("\n");
	return (0);
}
